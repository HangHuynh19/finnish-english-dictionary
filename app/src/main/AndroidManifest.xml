<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.hanghuynh.finnish_englishslangdictionary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinnishEnglishSlangDictionary">
        <activity android:name=".InitialScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".takeAQuiz.AnnounceQuizResults" />
        <activity android:name=".searchActivity.SearchResultsActivity" />
        <activity android:name=".progressActivity.ProgressActivity" />
        <activity android:name=".searchActivity.SearchActivity" />
        <activity android:name=".takeAQuiz.TakeAQuizActivity" />
        <activity android:name=".wordOfTheDayFeature.AlarmsetActivity" />
        <activity android:name=".MainActivity">
        </activity>

        <activity android:name=".MatchingActivity"/>
        <activity android:name=".wordOfTheDayFeature.NotificationActivity"/>
        <receiver android:name=".wordOfTheDayFeature.AlarmReceiver"/>
    </application>

</manifest>